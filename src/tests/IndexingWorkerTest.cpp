#define CATCH_CONFIG_MAIN
#include <catch2/catch_test_macros.hpp>
#include <QCoreApplication>
#include <memory>
#include "IndexingWorker.h"
#include "Library.h"
#include "Model.h"
#include <filesystem>

TEST_CASE("IndexingWorker Basic Initialization", "[IndexingWorker]") {
    // int argc = 0;
    // char* argv[] = {nullptr};
    // QCoreApplication app(argc, argv);

    // std::string testDir = "./temp_test_library";
    // std::filesystem::create_directories(testDir);

    // auto model = std::make_unique<Model>(testDir);
    // REQUIRE(model != nullptr);

    // Library library("Test Library", testDir.c_str());
    // library.model = model.get();

    // IndexingWorker worker(&library);
    // REQUIRE(&worker != nullptr);

    // std::filesystem::remove_all(testDir);
    //implement me


}
